import 'package:flutter/material.dart';

import 'constants.dart';

class EasyLocale{
  static final all = [
    const Locale(Constants.uz),
    const Locale(Constants.ru),
    const Locale(Constants.en),
  ];
}